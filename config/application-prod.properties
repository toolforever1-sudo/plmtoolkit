# ==========================================================================
# TEMPLATE ONLY — do not put real values in this file (it is tracked in git).
# Real production values live in the external config on the server:
#   D:/plm-toolkit/config/application.properties
# passed at launch via --spring.config.additional-location (see run-loop.bat).
# ==========================================================================

# Production database
spring.datasource.url=jdbc:oracle:thin:@//your-db-host:1521/your-service-name
spring.datasource.username=your_db_user
spring.datasource.password=REPLACE_ME

# Server
server.port=8090

# === Single/Sole Source Report (SharePoint upload via Microsoft Graph) ===
app.singlesole.sharepoint.enabled=false
app.singlesole.sharepoint.drive.id=REPLACE_ME
app.singlesole.graph.tenant.id=REPLACE_ME
app.singlesole.graph.client.id=REPLACE_ME
app.singlesole.graph.client.secret=REPLACE_ME
app.singlesole.graph.username=REPLACE_ME
app.singlesole.graph.password=REPLACE_ME
app.singlesole.graph.authority=https://login.microsoftonline.com/REPLACE_ME
